# TCP 三次握手

## 1. 什么是三次握手？

TCP 建立连接时，客户端和服务器需要交换三个报文段，这个过程称为**三次握手**（Three-Way Handshake）。

## 2. 三次握手的过程

1. **第一次握手（SYN）**  
   客户端发送 SYN 报文，并进入 SYN_SENT 状态。  
   - 报文中带有客户端的初始序列号 `seq = x`

2. **第二次握手（SYN + ACK）**  
   服务器收到 SYN 后，回复 SYN + ACK 报文，并进入 SYN_RCVD 状态。  
   - 确认号 `ack = x + 1`
   - 服务器自己的初始序列号 `seq = y`

3. **第三次握手（ACK）**  
   客户端收到服务器的 SYN + ACK 后，发送 ACK 报文，进入 ESTABLISHED 状态。  
   服务器收到这个 ACK 后，也进入 ESTABLISHED 状态。  
   - 确认号 `ack = y + 1`

此时，双方连接建立成功，可以开始传输数据。

## 3. 三次握手的目的与意义

1. **确认双方的发送和接收能力**
   - 第一次：客户端能发
   - 第二次：服务器能收、能发
   - 第三次：客户端能收
   - 最终确认网络双向都是通的

2. **同步序列号（Synchronize Sequence Numbers）**
   - TCP 是可靠的字节流协议，每个字节都有序号
   - 双方必须告诉对方自己的初始序列号（ISN），并得到确认
   - 之后才能正确进行确认、重传和排序

3. **防止旧的连接请求报文突然到达**
   - 网络中可能存在延迟很久的旧 SYN 包
   - 如果只用两次握手，服务器可能错误地建立连接
   - 三次握手要求客户端再发一次 ACK，旧的无效请求就无法完成握手

4. **以较小的代价建立全双工连接**
   - 两次不够可靠，四次又多余，三次是效率和可靠性的平衡

## 4. 为什么不是两次握手？

如果只有两次握手：
- 服务器发出 SYN + ACK 后就认为连接建立了
- 但此时还无法确认客户端是否真的收到了这个报文
- 也无法有效防止已经失效的旧连接请求

## 5. 简单总结

三次握手的核心就是：

> 用三次报文交互，可靠地确认双方都具备收发能力，同步初始序列号，并防止旧连接请求带来的问题，从而建立起可靠的全双工连接。
